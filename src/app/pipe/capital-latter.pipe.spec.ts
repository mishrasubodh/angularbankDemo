import { CapitalLatterPipe } from './capital-latter.pipe';

describe('CapitalLatterPipe', () => {
  it('create an instance', () => {
    const pipe = new CapitalLatterPipe();
    expect(pipe).toBeTruthy();
  });
});
